# Private key of the DataCache contract owner (must be the same wallet that deployed)
# This key is used to call storeResult() and recordHits() on-chain
PRIVATE_KEY=0x...

# Optional: for AI queries (type "what is the speed of light")
GROQ_API_KEY=
