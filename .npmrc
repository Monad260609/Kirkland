# Match Yarn's permissive peer-dependency resolution. Several deps
# (next-themes, qrcode.react, burner-connector…) don't declare React 19
# in their peers yet, but the resolved tree is the one the app was
# built and tested with. Remove once upstream peers catch up.
legacy-peer-deps=true
